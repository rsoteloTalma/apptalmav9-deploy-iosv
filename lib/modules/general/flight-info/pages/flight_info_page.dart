import 'package:flutter/material.dart';

class FlightInfoPage extends StatelessWidget {
  const FlightInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Buscador Vuelos'),
      ),
    );
  }
}
